[project]
name = "growpido"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Suyash Zinjurke", email = "zinjurke77h@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
growpido = "growpido:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

